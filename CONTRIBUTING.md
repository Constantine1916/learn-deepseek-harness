# Contributing

## 工作方式

Learn DeepSeek Harness 采用规格驱动开发：

1. 从 [SPEC.md](SPEC.md) 找到当前规格。
2. 确认需求 ID、设计约束和验收标准。
3. 在实施计划中选择一个可独立验证的任务。
4. 先补充或更新覆盖该行为的测试。
5. 实现最小完整能力。
6. 运行与变更表面匹配的检查。
7. 在变更说明中列出需求 ID、测试命令和验收证据。

## 架构原则

- 教学行为通过 DSH 插件、事件、工具、preset 和 bundle 实现。
- 扩展插件依赖公开 Service Definition，不依赖具体 Provider。
- 模型可见信息必须能够从持久事件或版本化课程输入重建。
- 学习进度由证据投影得出，不以一段不可审计的自由文本作为唯一状态。
- 确定性检查负责可以机械判断的正确性；模型负责解释、追问和反馈组织。
- 默认在隔离练习工作区中运行学生代码。
- 课程与支持的 DSH 版本显式绑定。

## 文档要求

- 使用当前状态描述，不记录讨论过程。
- 一个事实只保留一个权威位置，其他位置使用链接。
- 需求使用 Must、Should、May 表示优先级。
- 每个验收项必须能够由命令、快照、事件记录或人工 rubric 复核。

## 提交前

首个代码里程碑建立后，本节将列出精确的格式化、类型检查、单元测试、快照、端到端和构建命令。在此之前，至少运行：

~~~sh
git diff --check
~~~

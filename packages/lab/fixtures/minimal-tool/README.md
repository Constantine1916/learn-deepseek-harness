# Minimal Tool exercise

Complete `tool.ts` with a model-callable `greet` tool. Keep greeting behavior in an exported pure function, define input/output schemas, render the canonical output, and register through `ctx.tools`.

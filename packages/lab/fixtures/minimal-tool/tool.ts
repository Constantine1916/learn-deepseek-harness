import type { Context } from '@deepseek-ai/cordis'
import { defineTool } from '@deepseek-ai/dsh-tools'

// TODO: export a pure greet(name) function.
// TODO: define a greet tool with a required string name and structured greeting output.
// TODO: render the output and register it through ctx.tools in apply(ctx).

void defineTool
void (null as unknown as Context)

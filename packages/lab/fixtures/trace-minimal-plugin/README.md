# Trace a minimal DSH plugin

Fill `answer.json` with your own explanation of the Plugin → Context → Service/event registration → Effect/disposer lifecycle. Cite at least two of the source files supplied by the current curriculum unit.

The deterministic check validates the artifact shape and source citations. It does not decide whether the explanation is conceptually strong; that remains authored evidence evaluated against the unit rubric.

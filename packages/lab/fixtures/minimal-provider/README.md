# Minimal Provider exercise

Complete `provider.ts` so it declares a `clock` Service Definition, implements `LocalClock`, and exposes a Consumer that depends only on `ctx.clock`. Run the curriculum check after editing.

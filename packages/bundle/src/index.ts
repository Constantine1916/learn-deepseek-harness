/**
 * Learn DSH bundle metadata package.
 *
 * Runtime behavior is owned by plugins referenced from `cordis.patch.yml`.
 *
 * @module @learn-dsh/bundle
 */

/** Published package name used by profile tooling. */
export const BUNDLE_PACKAGE_NAME = '@learn-dsh/bundle'
